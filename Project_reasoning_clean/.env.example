# No real credentials are needed for current local Ollama configs.
# Endpoint/model and placeholder api_key=ollama are configured in YAML.
# Current wrapper loads /app/setting.env, not this file; do not rely on auto-loading.
PYTHONUTF8=1
PYTHONDONTWRITEBYTECODE=1
